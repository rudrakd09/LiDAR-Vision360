# Cloud Backend & Dashboard

## Status

Not yet implemented. Planned for **Phase 12** (backend, `cloud/backend/`), **Phase 13**
(database schema), and **Phase 14** (dashboard, `cloud/dashboard/`).

Will document: the FastAPI backend's REST/WebSocket surface for real-time telemetry, data
validation, and health monitoring; the PostgreSQL schema for vehicles, LiDAR scans, detected
objects, object observations, safety events, sensor health, and system events (with time-series
considerations); and the React + TypeScript dashboard's real-time environment view, safety panel,
object list, system-health panel, and historical analytics (Phase 14/15 for alerts).
