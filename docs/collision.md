# Collision & Clearance Engines

## Status

Not yet implemented. Planned for **Phase 9** (collision, `perception/src/collision/`) and
**Phase 10** (clearance, `perception/src/clearance/`).

Will document: configurable vehicle dimensions and safety margins; minimum obstacle distance,
safety-zone, relative-velocity, and Time-to-Collision calculations; the `SAFE` / `WARNING` /
`CRITICAL` risk states and how false alarms for stationary out-of-path objects are avoided; and
the front/rear/left/right clearance, corridor-width calculations, and `SAFE` / `CAUTION` /
`LOW_CLEARANCE` / `CRITICAL` clearance states.
